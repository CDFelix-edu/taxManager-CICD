name: Auto create pull request

on:
  push:
    branches-ignore:
      - main
      - 'auto-update-*'

jobs:
  auto-create-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build and Verify
        run: mvn -B clean verify

      - name: Create pull request
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          USER_MSG=$(git log -1 --pretty=%B)
          echo "DEBUG: Messaggio rilevato: $USER_MSG"
          echo "msg=$USER_MSG" >> $GITHUB_OUTPUT
          git add .
          
          BRANCH_NAME="auto-update-${{ github.run_id }}"
          git checkout -b $BRANCH_NAME
          git commit --allow-empty -m "${USER_MSG:-Auto update from pipeline}"
          git push origin $BRANCH_NAME
          echo "NEW_BRANCH=$BRANCH_NAME" >> $GITHUB_ENV
          echo "USER_MSG=$USER_MSG" >> $GITHUB_ENV

      - name: Create PR via CLI
        if: env.NEW_BRANCH != ''
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
        run: |
          gh pr create \
            --title "${{ env.USER_MSG }} (Build #${{ github.run_number }})" \
            --body "Pipeline di verifica completata con successo." \
            --base main \
            --head ${{ env.NEW_BRANCH }}
