name: Manual Version Release

on:
  push:
    branches:
      - main

jobs:
  release:
    # Si attiva se trova RELEASE (v...)
    if: contains(github.event.head_commit.message, 'RELEASE (v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract Version from Commit
        id: get_version
        run: |
          # Estrae il testo dentro le parentesi dopo RELEASE
          # Esempio: RELEASE (v1.2.3) -> v1.2.3
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          VERSION=$(echo "$COMMIT_MSG" | grep -oP 'RELEASE \(\K[^\)]+')
          echo "Versione estratta: $VERSION"
          echo "tag=$VERSION" >> $GITHUB_OUTPUT

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build JAR
        run: mvn -B clean package -DskipTests

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.get_version.outputs.tag }}
          name: "Release ${{ steps.get_version.outputs.tag }}"
          body: |
            ### 🚀 Nuova Release Manuale
            **Versione:** `${{ steps.get_version.outputs.tag }}`
            **Messaggio:** ${{ github.event.head_commit.message }}
          files: target/*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}